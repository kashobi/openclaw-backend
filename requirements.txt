# Pinned to known good releases confirmed by the resolver. Keeps builds deterministic
# so a surprise upstream release cannot break a deploy that worked before.
flask==3.1.3
flask-cors==6.0.5
flask-limiter==4.1.1
yfinance==1.5.1
requests==2.33.1
gunicorn==26.0.0
psycopg2-binary==2.9.12
stripe==15.3.0
akshare
